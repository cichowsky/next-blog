import { SWrapper, SInner } from './Header.styles';

const Header = () => {
  return (
    <SWrapper>
      <SInner>
        {/* LOGO */}
        {/* NAV */}
        Header
      </SInner>
    </SWrapper>
  );
};

export default Header;
